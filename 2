#include <stdio.h>
#include <math.h>
int main() {
    int n,s=0;
    scanf_s("%d",&n);

    for (int i=0;i<5;i++){
        s+=(n%10)*pow(10,(5-i-1));
        n=n/10;
    }
    printf("%d",s);
    return 0;
}
